---
layout: layouts/soldier.njk
title: Larry Kleinschmidt
slug: kleinschmidt-larry
breadcrumb: Larry Kleinschmidt
permalink: /soldiers/kleinschmidt-larry/
draft: true   # NOT visible on the site — third-party photo contributor, NOT a documented D Co, 2/8 Cav soldier. Exists only as a filing point for his photos (see photos/locations/fsb-st-barbara/). Do not remove draft unless his own service/unit is separately researched and confirmed.
tags:
  - soldier

# ── IDENTITY ──────────────────────────────────
first_name: Larry
last_name: Kleinschmidt
middle_name:
suffix:
nickname:
birth_year:

# ── RANK & ASSIGNMENT ─────────────────────────
rank:
mos:
platoon:

# ── SERVICE ───────────────────────────────────
arrived:
departed:
character_of_service:
status:

# ── POST-SERVICE ──────────────────────────────
hometown:
current_location:
year_deceased:
cause_of_death:

# ── PROFILE PHOTO ─────────────────────────────
profile_photo:

# ── DECORATIONS ────────────────────────────────
decorations:

distinguished_decorations:

decorations_unconfirmed:

# ── SERVICE RECORD ──────────────────────────────
service_record:
  induction:
    status:
    location:
    date:
  assignments:

# ── CONTACT ───────────────────────────────────
share_contact: false
contact:
  name:
  relation:
  last_verified:

# ── EXTERNAL LINKS ─────────────────────────────
links:
  wall:
  other:
    - label: "Personal photo gallery — \"The French Fort\" (VietnamGallery17)"
      url: "http://www.larrykleinschmidt.com/VietnamGallery17.htm"

# ── TIMELINE SOURCE NOTE ──────────────────────────
timeline_source: >
  Not a researched D Company, 2/8 Cavalry veteran profile. Larry Kleinschmidt is a
  third-party photo contributor — his unit, dates of service, and any connection to D
  Company are not established. This stub exists only to file photo credits/metadata
  under the archive's standard soldier-photo pipeline. If Kleinschmidt's own Vietnam
  service is ever researched and confirmed, this profile can be expanded and published.

# ── SERVICE TIMELINE ─────────────────────────────
timeline:

# ── PHOTOS ────────────────────────────────────
photo_intro: >
  Photographs contributed from Larry Kleinschmidt's personal Vietnam photo gallery
  ("The French Fort," larrykleinschmidt.com/VietnamGallery17.htm), relating to FSB
  Saint Barbara. See locations/fsb-st-barbara/ for how these are used.

wartime_content_notice: false

photos:

# ── DOCUMENTS ──────────────────────────────────
documents:

# ── RELATED ───────────────────────────────────
brothers:

related_events:

# ── ADMIN ───────────────────────────────────────
date_added: 2026-07-23
last_updated: 2026-07-23
contributed_by: Michael
notes: >
  DRAFT (draft: true) — deliberately NOT published. This is not a soldier profile in
  the normal sense: Larry Kleinschmidt has no established connection to D Company, 2nd
  Battalion, 8th Cavalry. He runs a personal website (larrykleinschmidt.com) with a
  Vietnam photo gallery; Michael identified VietnamGallery17.htm as the specific gallery
  relating to FSB Saint Barbara, which Kleinschmidt's own page titles "The French Fort"
  — independently matching that base's alias in the FSB-locations gazetteer (see
  locations/fsb-st-barbara/).
  WHY THIS STUB EXISTS: the site's photo pipeline (site/_data/photosBySlug.js /
  photosByFsb.js) only crawls site/soldiers/[slug]/photos/**; it does not read
  site/locations/[slug]/photos/. Location-tied photos from a contributor are meant to
  live at site/soldiers/[contributor-slug]/photos/locations/[loc-slug]/index.md, each
  entry carrying fsb: <loc-slug>, which the location page's own template picks up
  automatically via photosByFsb[slug] to populate its Photos tab. This stub is the
  minimum needed to give Kleinschmidt a soldier-slug directory for that pipeline to
  find, without representing him as a company veteran or putting an unresearched page
  on the live site.
  Four photos filed at photos/locations/fsb-st-barbara/: two show base structures (one
  aerial-ish overview with a gun/mortar pit, one ground-level shot with a Quonset hut
  and sandbagged bunker), one shows a bunker decorated with a small Christmas tree
  (filename dates it Dec 1968), and one shows Nui Ba Den from open ground nearby.
  Original photographer's captions were not retrieved — a WebFetch attempt on the
  gallery page returned no usable content, and Claude in Chrome wasn't connected in
  that session — so captions in the photo index are archive-written descriptions of
  what's visible, flagged as such.
  If Kleinschmidt's own Vietnam service is ever researched (unit, dates, rank), this
  profile should be rebuilt properly and draft removed only then — per the standing
  "ask before creating/publishing" rule, check with Michael first either way.
---
